[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "live: tests that require opt-in access to the real Hargassner Cloud API",
]

[tool.mypy]
explicit_package_bases = true

[tool.ruff]
target-version = "py312"
